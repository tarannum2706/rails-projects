<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
<div class="container-fluid">
  <a class="navbar-brand" href=#>The Blog App</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item hover">
        <a class="nav-link" href="/posts">Posts</a>
      </li>
      <li class="nav-item hover">
        <a class="nav-link" href="/posts/new">Add New Post</a>
      </li>
      </ul>
    <% if user_signed_in? %>
          <%= link_to "Edit", edit_user_registration_path,:class => 'navbar-link',:class => 'btn btn-warning' %>  |
          <%= button_to "Logout", destroy_user_session_path, method: :delete,:class => 'btn btn-warning'%>
      <% else %>
          <%= link_to "Sign up", new_user_registration_path,:class => 'navbar-link',:class => 'btn btn-warning' %>  |
          <%= link_to "Login", new_user_session_path, :class => 'navbar-link',:class => 'btn btn-warning' %>
      <% end %>
    </div>
 </div>
</nav>
      <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
<br/>
      <div class="container">
  <% if notice %>
    <p class="alert alert-info"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-danger"><%= alert %></p>
  <% end %>
</div>
<br>
<div class="container">
    <%= yield %>
    </div>
  </body>
</html>
